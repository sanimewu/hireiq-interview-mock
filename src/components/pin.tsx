import type {Interview} from "@/lib";

interface InterviewPinProps {
    interview: Interview;
}
const Pin = ({interview}:InterviewPinProps) => {
    return (
        <div>
            Interview;
        </div>
    );
};

export default Pin;
